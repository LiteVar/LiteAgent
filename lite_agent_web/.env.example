# Base URL for the API, specifying the backend server address and port.
# Used to proxy requests to the backend service in the development environment.
VITE_API_BASE_URL=""

# API path prefix, used to dynamically replace the base path in requests with the actual backend path.
# For example, it proxies "/v1" to "/liteAgent/v1".
VITE_API_PATH=""
